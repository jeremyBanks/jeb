[package]
name = "partli"
version = "0.1.0"
edition = "2021"
description = "Partli: A Particle-Life Cellular Automaton"
license = "MIT"

[dependencies]
rand = "0.8"

[dev-dependencies]
rand_chacha = "0.3"

// [recovery] edit target not found, appending:
[package]
name = "gravity"
version = "0.1.0"
edition = "2021"

[lib]
name = "partli"
path = "src/lib.rs"

[[bin]]
name = "gravity"
path = "src/main.rs"

[dependencies]
png = "0.17"
ctrlc = { version = "3", features = ["termination"] }
rand = "0.8"

[dev-dependencies]
rand_chacha = "0.3"
