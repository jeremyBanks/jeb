HEAD: 1
1:
  message: Initial commit
  tree:
    README.md: '# Project'
    src:
      lib.rs: pub fn main() {}

staged:
  README.md: '# Project v0.1'
  src:
    lib.rs: pub fn main() { println!("staged"); }
  tests:
    test.rs: '#[test] fn test_it() {}'

# working reuses some files from HEAD (via dedup) and modifies others
working:
  README.md:
    //commit: 1
    //path: README.md
  src:
    lib.rs: pub fn main() { println!("working"); }
  tests:
    test.rs: '#[test] fn test_it() {}'  # Same content as staged - dedup should find it
  NOTES.txt: Working notes
