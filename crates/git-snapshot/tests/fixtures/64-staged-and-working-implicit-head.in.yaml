# Files in implicit commit, staged changes, working changes
# No explicit HEAD or refs - all inferred

1:
  tree:
    Cargo.toml: "[package]"
    src/lib.rs: "pub fn foo() {}"

staged:
  src/lib.rs: "pub fn foo() { /* updated */ }"

working:
  src/lib.rs: "pub fn foo() { /* wip */ }"
  tests/test.rs: "#[test] fn it_works() {}"
