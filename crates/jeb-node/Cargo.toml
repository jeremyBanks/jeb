[package]
name = "jeb-node"
publish = true
description = "(part of https://crates.io/crates/jeb)"
keywords = []
categories = []
edition.workspace = true
include.workspace = true
license.workspace = true
repository.workspace = true
version.workspace = true

[features]
default = ["wasm", "stdio", "fs", "json"]
fs = ["tokio/fs"]
json = []
stdio = ["tokio/io-std"]
wasm = ["dep:wasm-bindgen"]

[dependencies]
jeb-values = { path = "../jeb-values" }
jeb-common = { path = "../jeb-common" }
derive_more = { version = "2.0.1", features = ["full"] }

tokio = { version = "1.0", features = ["rt", "macros", "sync", "io-util"] }
tokio-stream = "0.1"
tokio-util = { version = "0.7.17", features = ["codec", "io"] }
wasm-bindgen = { workspace = true, optional = true }
static_assertions = "1.1.0"
