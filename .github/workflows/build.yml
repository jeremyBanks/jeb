# DO NOT EDIT - Generated by `cargo run --bin _ci generate-workflows`
# Edit crates/_ci/src/workflows.rs instead

permissions:
  contents: read
on:
  workflow_dispatch: null
  pull_request: null
  push:
    branches:
      - trunk
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - run: cargo run --bin _ci build
  build-warnings:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - uses: actions/checkout@v5
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          rustflags: ""
      - run: cargo run --bin _ci build-warnings
