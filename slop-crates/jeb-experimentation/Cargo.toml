[package]
name = "jeb-experimentation"
version = "0.1.0"
edition = "2021"

# Empty workspace table keeps this crate independent from parent workspace
[workspace]

[dependencies]
futures = "0.3"
parking_lot = "0.12"
